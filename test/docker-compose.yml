version: '3'
services:
  php:
    build:
      context: $PWD
      dockerfile: test/Dockerfile
    volumes:
    - $PWD:/code
    depends_on:
    - redis
  redis:
    image: redis:${REDIS_VERSION}-alpine
